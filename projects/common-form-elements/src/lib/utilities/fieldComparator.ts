export const FieldComparator  = {
  operators : {
    '<': (a, b) => a < b,
    '>': (a, b) => a > b,
    '<=': (a, b) => a <= b,
    '>=': (a, b) => a >= b,
    '===': (a, b) => a === b
  }
};


// export class Dummy {
//   operator: any = {
//   };
// }
